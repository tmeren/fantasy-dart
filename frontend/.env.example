# Fantasy Darts Frontend Configuration
# Copy to .env.local and fill in values

# Backend API URL — leave unset for local dev (proxies to localhost:8000)
# NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app
